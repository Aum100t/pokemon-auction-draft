{
  "rules": {
    "archives": {
      ".read": true,
      "$archiveId": {
        ".write": "auth != null && auth.token.email_verified === true && (auth.token.email === 'chanatun73344@gmail.com' || root.child('admins').child(auth.token.email.toLowerCase().replace('.', ',')).child('role').val() === 'owner' || ((root.child('admins').child(auth.token.email.toLowerCase().replace('.', ',')).child('role').val() === 'organizer' || root.child('admins').child(auth.token.email.toLowerCase().replace('.', ',')).child('role').val() === 'creator') && ((!data.exists() && newData.child('creatorUid').val() === auth.uid) || (data.exists() && data.child('creatorUid').val() === auth.uid))))"
      }
    },
    "admins": {
      ".read": "auth != null && auth.token.email_verified === true",
      "$emailKey": {
        ".write": "auth != null && auth.token.email_verified === true && (auth.token.email === 'chanatun73344@gmail.com' || root.child('admins').child(auth.token.email.toLowerCase().replace('.', ',')).child('role').val() === 'owner')"
      }
    },
    "roleRequests": {
      ".read": "auth != null && auth.token.email_verified === true && (auth.token.email === 'chanatun73344@gmail.com' || root.child('admins').child(auth.token.email.toLowerCase().replace('.', ',')).child('role').val() === 'owner')",
      "$emailKey": {
        ".write": "auth != null && auth.token.email_verified === true && ((!data.exists() && newData.child('uid').val() === auth.uid && newData.child('email').val() === auth.token.email.toLowerCase() && newData.child('status').val() === 'pending') || auth.token.email === 'chanatun73344@gmail.com' || root.child('admins').child(auth.token.email.toLowerCase().replace('.', ',')).child('role').val() === 'owner')"
      }
    },
    "rooms": {
      ".read": "auth != null && auth.token.email_verified === true",
      "$roomId": {
        ".write": "auth != null && auth.token.email_verified === true && ((!data.exists() && newData.child('creatorUid').val() === auth.uid && (auth.token.email === 'chanatun73344@gmail.com' || root.child('admins').child(auth.token.email.toLowerCase().replace('.', ',')).child('role').val() === 'owner' || root.child('admins').child(auth.token.email.toLowerCase().replace('.', ',')).child('role').val() === 'organizer' || root.child('admins').child(auth.token.email.toLowerCase().replace('.', ',')).child('role').val() === 'creator')) || (data.exists() && data.child('memberUids').child(auth.uid).val() === true) || auth.token.email === 'chanatun73344@gmail.com')",
        "memberUids": { "$uid": { ".write": "auth != null && auth.uid === $uid" } }
      }
    },
    "userHistory": { "$uid": { ".read": "auth != null && auth.uid === $uid", ".write": "auth != null && auth.uid === $uid" } }
  }
}
